<h2 style="padding-left:20px;padding-bottom:10px;"><%= @todolist.title %></h2>

<div style="padding-left:20px;padding-right:20px;">
  <table class="table table-bordered table-hover">
    <tr>
      <td>須完成日期：<%= @todolist.due_date %></td>
    </tr>
    <tr>
      <td>詳細內容：<%= @todolist.description %></td>
    </tr>
  </table>
</div>

<div style="padding-left:20px;">
<%= link_to '回到我的待辦清單', todolists_path %>
</div>
